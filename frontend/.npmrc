# @privy-io/wagmi pins viem to exactly 2.52.0, but the project (and other
# Privy packages) resolve viem to a newer 2.5x. yarn installs this fine, but
# strict `npm install` (used by Vercel) errors with ERESOLVE. Accept the same
# dependency tree that already works locally.
legacy-peer-deps=true
